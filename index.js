import { getAndRemoveByIndex } from '@writetome51/array-get-and-remove-by-index';


// index can be negative or positive.

export const removeByIndex = (index, array) => {
    getAndRemoveByIndex(index, array);
}
