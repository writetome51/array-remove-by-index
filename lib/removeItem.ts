import { getAndRemoveItem } from '@writetome51/array-get-and-remove-item';


// index can be negative or positive.

export function removeItem(index, array): void {
	getAndRemoveItem(index, array);
}
